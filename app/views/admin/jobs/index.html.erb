<header>
  <h2 class="left">線上徵才<span> / 首頁管理</span></h2>
  <nav class="left"></nav>
</header>
<article>
  <!-- <div class="hgroup">
    <div class="button right"> -->
      <%# link_to( admin_jobs_path(@job), :method => :post, class: "right highlight") do %>
        <!-- 新增標籤<img src="/images/admin/add.png"> -->
      <%# end %>
    <!-- </div>
  </div> -->
  <div class="list">
    <table width="100%" border="0" cellspacing="0" cellpadding="0">
      <% @jobs.each do |job| %>
        <tr <%= "class=off" if job.status == "disable" %> >            
          <td width="5%" class="icon"></td>
          <td width="12%"><%= link_to "#{job.name}", job.websiteurl , target: "_blank" %></td>
          <td width="71%"><%= job.websiteurl %></td>
          <td width="12%">
            <div class="tool">
              <%= link_to '', edit_admin_job_path(job),class: "edit" , title: "編輯" %>
              <%# link_to '', admin_job_path(job), method: :delete, data: { confirm: 'Are you sure?' }, class: 'delete', title: '刪除' %>
            </div>
          </td>
        </tr>
      <% end %>
    </table>
  </div>
</article>